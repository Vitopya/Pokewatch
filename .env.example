# Google Gemini API key — server-side only, never exposed to the browser.
# Get yours at https://aistudio.google.com/apikey (free tier available)
GEMINI_API_KEY=

# Optional: override default model
# GEMINI_MODEL=gemini-2.5-flash
